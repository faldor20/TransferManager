{
  #example:
  #{
  #  "GroupName": "Test1",
  #  "Source": "./testSource",
  #  "Destination": "***REMOVED***@/testdest",
  #  "IsFTP": true,
  #},
  # Paths must be 1 / or 2 \\ a single \ is an escape character and will cause an error
  #Any if you do not wish to enable ftp do not include it in the config
  "ClientName": "TestClient",
  "ManagerIP": "localhost",
  "MaxJobs":{"Main":3,"Eli":2},
  "WatchDirs":
    [
      {"GroupList":["Main","Eli"],
        "DirData":
          {
            "SourceDir": "./testSource/",
            "DestinationDir": "H:/testDest/",
            "DeleteCompleted":false,
          },

        # "TranscodeData":
          # {
            # "TranscodeExtensions": ["mp4", "mxf"],
            # "FfmpegArgs": " -c:v h264 -crf 18 -pix_fmt +  -movflags frag_keyframe+empty_moov -g 52  -preset fast -flags +ildct+ilme -f mp4 ",
            # "OutputFileExtension": "mp4",
          # },
      },
    ],
}
